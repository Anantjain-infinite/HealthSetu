/**
 * @file src/modules/video/signalling.ts
 * @description Socket.io WebRTC signalling handlers.
 * Full implementation comes in Step 5 (Consultation module).
 * This stub allows the server to start without import errors.
 */

import type { Server as SocketIOServer } from 'socket.io';

/**
 * Registers all Socket.io event handlers for WebRTC signalling
 * and in-call chat on the provided Socket.io server instance.
 *
 * @param io  The Socket.io Server instance from server.ts
 */
export function registerSignallingHandlers(_io: SocketIOServer): void {
  // Full implementation added in Step 5
}
